apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: java-exchange-rate         # Unique name, lowercase-with-dashes
  description: My existing Java Spring Boot application for XYZ feature
  tags:
    - java
    - spring-boot
    - backend
  annotations:
    github.com/project-slug: Ovvio23/java-exchange-rate   # Helps with links
    backstage.io/techdocs-ref: dir:.                     # If you have docs in /docs folder
spec:
  type: service                # or library, website, etc.
  lifecycle: production        # or experimental, deprecated
  owner: your-team-or-user     # e.g. team:java-squad or user:jacky
  # Optional: system: payment-services
